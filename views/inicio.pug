extends ./layout/index

block contenido
  section(class="bg-cover bg-center h-screen" style="background-image: url('/img/superior.jpg');")
    div(class="container mx-auto text-center text-white flex flex-col justify-center items-center h-full")
      h1(class="text-5xl font-bold") #{nombreSitio}
      p(class="mt-4 text-xl") Explora el mundo con nosotros.
      a(href="/viajes" class="mt-6 bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform transition-transform duration-300 hover:scale-105 hover:shadow-2xl") Ver Viajes
  section(class="my-10")
    h1(class="text-4xl font-bold mb-6 text-center") Viajes Destacados
    div(class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 p-6 max-w-7xl mx-auto")
      include ./layout/bloqueViajes
  h1(class="text-4xl font-bold mb-6 text-center") Contáctanos
  include ./layout/formulario
